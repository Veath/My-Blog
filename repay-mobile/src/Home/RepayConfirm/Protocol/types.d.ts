export interface CheckboxProps {
    checked: boolean,
    handleCheck: () => void,
}

export interface ShowProtocolProps {
    checked: boolean,
    readProtocol: () => void,
    handleCheck: () => void,
}
